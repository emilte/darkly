- On inspect le footer du site et on trouve des liens vers plusieurs réseaux sociaux.

- On voit que les liens renvoi vers l'index du site avec des parametres dans l'url (page et site).

- On modifi le parametre site, ce qui nous donne le flag. Ceci veut dire que les parametres rentré dans l'URl ne sont pas checké.